<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface"
    android:clipToPadding="false"
    android:fitsSystemWindows="true"
    tools:context=".presentation.ui.fragment.HomeFragment">

    <!-- Bu Layout düymələri saxlayır -->
    <LinearLayout
        android:id="@+id/buttonsLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="10dp"
        android:paddingTop="10dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- 1-ci SƏTİR: 3 DÜYMƏ (Ən çox baxılan, Kalkulyator, Avtokataloq) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:baselineAligned="false"
            android:layout_marginBottom="8dp">

            <!-- Düymə 1 -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnMostViewed"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginEnd="4dp"
                android:backgroundTint="@android:color/white"
                android:text="Ən çox baxılan"
                android:textSize="11sp"
                android:padding="0dp"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:textColor="@android:color/black"
                android:textAllCaps="false"
                app:icon="@drawable/flame"
                app:iconTint="@null"
                app:iconGravity="textStart"
                app:iconSize="16dp"
                app:strokeColor="#E0E0E0"
                app:strokeWidth="1dp"/>

            <!-- Düymə 2 -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnCalculator"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginEnd="4dp"
                android:backgroundTint="@android:color/white"
                android:text="Kalkulyator"
                android:textSize="11sp"
                android:padding="0dp"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:textColor="@android:color/black"
                android:textAllCaps="false"
                app:icon="@drawable/calculator"
                app:iconTint="@null"
                app:iconGravity="textStart"
                app:iconSize="16dp"
                app:strokeColor="#E0E0E0"
                app:strokeWidth="1dp"/>

            <!-- Düymə 3 -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnCatalog"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:backgroundTint="@android:color/white"
                android:text="Avtokataloq"
                android:textSize="11sp"
                android:padding="0dp"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:textColor="@android:color/black"
                android:textAllCaps="false"
                app:icon="@drawable/catalog"
                app:iconTint="@null"
                app:iconGravity="textStart"
                app:iconSize="16dp"
                app:strokeColor="#E0E0E0"
                app:strokeWidth="1dp"/>
        </LinearLayout>

        <!-- 2-ci SƏTİR: 2 DÜYMƏ (Bu gün, Dilerlər) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:baselineAligned="false">

            <!-- Düymə 4 -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnToday"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginEnd="4dp"
                android:backgroundTint="@android:color/white"
                android:text="Bu gün: 1781"
                android:textColor="@android:color/black"
                android:textAllCaps="false"
                app:icon="@drawable/bmw_icon"
                app:iconTint="@null"
                app:iconGravity="textStart"
                app:strokeColor="#E0E0E0"
                app:strokeWidth="1dp"/>

            <!-- Düymə 5 -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnDealers"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="4dp"
                android:backgroundTint="@android:color/white"
                android:text="Dilerlər"
                android:textColor="@android:color/black"
                android:textAllCaps="false"
                app:icon="@drawable/lamborghini"
                app:iconTint="@null"
                app:iconGravity="textStart"
                app:strokeColor="#E0E0E0"
                app:strokeWidth="1dp"/>
        </LinearLayout>

    </LinearLayout>

    <com.google.android.flexbox.FlexboxLayout
        android:id="@+id/flexboxLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:flexDirection="row"
        app:justifyContent="space_between"
        app:flexWrap="nowrap"
        android:paddingHorizontal="8dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/buttonsLayout" />

    <LinearLayout
        android:id="@+id/imagesLayout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="?attr/colorSurface"
        app:layout_constraintTop_toBottomOf="@id/flexboxLayout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:padding="4dp">

        <ImageView
            android:id="@+id/image1"
            android:clickable="true"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="110dp"
            android:src="@drawable/tap"
            android:layout_marginEnd="8dp" />

        <ImageView
            android:id="@+id/image2"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="110dp"
            android:src="@drawable/bina4" />

    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="?attr/colorSurface"
        app:layout_constraintTop_toBottomOf="@id/imagesLayout"
        app:layout_constraintBottom_toBottomOf="parent"
        android:paddingHorizontal="10dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>